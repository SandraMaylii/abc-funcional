services:
  - type: web
    name: habla-mano-app
    env: python
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    autoDeploy: true
    runtime: python
    pythonVersion: 3.10.11
